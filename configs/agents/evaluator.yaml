name: "Evaluator"
description: "This agent evaluates the quality of the response generated by the synthesizer and provides feedback on its accuracy, relevance, and completeness."
configs:
  model: "gemini-2.5-flash"
  prompt: "prompts/evaluator.txt"
